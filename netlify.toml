[build.environment]
  NPM_FLAGS = "--version"
  NODE_VERSION = "17.6.0"

[build]
  publish = "dist"
  command = "npm i && npm run build"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200